services:
  - type: web
    name: smart-campus-ai-module
    runtime: docker
    region: oregon
    plan: starter
    envVars:
      - key: GEMINI_API_KEY
        sync: false
      - key: GEMINI_MODEL
        value: "gemini-2.0-flash"
    healthCheckPath: /api/proctor/health
    autoDeploy: true
